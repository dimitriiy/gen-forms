import stylex from '@stylexjs/stylex';
export const classes = stylex.create({
  root: {},
  formBlock: {
    borderRadius: 8,
    border: '2px solid #e2e8f0',
    padding: '12px',
  },
  formBlockSpace: { marginBottom: '12px' },
  formBlockRow: {
    marginBottom: '12px',
  },

  formBlockRowItem: {
    marginBottom: '8px',
  },
});
